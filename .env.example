# Banco de dados (Supabase ou local)
# Supabase: use a connection string do projeto (Dashboard → Settings → Database)
# Se a senha tiver caracteres especiais (ex.: $), use aspas: "sua$senha"
# Na primeira vez, rode: cd backend && npm run db:migrate
DATABASE_URL=postgresql://postgres:8x$sKLy7kS7qlm@db.dgeqxfeucsyuxfhupujr.supabase.co:5432/postgres

# Opcional: para PostgreSQL local (Docker), use variáveis separadas em vez de DATABASE_URL
# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=rumo
# PGUSER=rumo
# PGPASSWORD=rumo_secret

# Porta da API (opcional)
# PORT=3001

# Autenticação (Fase 1 - local; altere em produção)
# JWT_SECRET=um-segredo-forte-aqui
# JWT_EXPIRES_IN=7d
